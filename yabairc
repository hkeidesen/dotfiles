#!/usr/bin/env sh

# Layout
yabai -m config layout                 bsp
yabai -m config window_placement       second_child
yabai -m config auto_balance           off
yabai -m config split_ratio            0.50

# Gaps
yabai -m config top_padding            0
yabai -m config bottom_padding         0
yabai -m config left_padding           0
yabai -m config right_padding          0
yabai -m config window_gap             12

# Mouse
yabai -m config mouse_drop_action      swap
yabai -m config mouse_modifier         alt
yabai -m config mouse_action1          move
yabai -m config mouse_action2          resize

# Rules — float these apps
for app in "System Settings" "Finder" "Preview" "Music" "1Password" "Messages" "Session" "Raycast" "Google Chrome for Testing"; do
  yabai -m rule --add app="^$app$" manage=off
done
yabai -m rule --add role="AXDialog" manage=off
yabai -m rule --add title="Preferences" manage=off

# WezTerm — always tiled
yabai -m rule --add app="^WezTerm$" manage=on

# Auto-focus WezTerm on space switch
yabai -m signal --add event=space_changed action='
  WT_ID=$(yabai -m query --windows --space | jq "[.[] | select(.app==\"WezTerm\" and .\"is-minimized\"==false)][0].id // empty" -r)
  [ -n "$WT_ID" ] && yabai -m window --focus "$WT_ID"
' 2>/dev/null
